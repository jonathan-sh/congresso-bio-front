
import React from 'react';
import LinearProgress from 'material-ui/LinearProgress';

const LinearProgressExampleSimple = () => (
  <LinearProgress min={0} max={1000} mode="indeterminate"/>
);

export default LinearProgressExampleSimple;
